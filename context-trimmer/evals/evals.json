{
  "skill_name": "context-trimmer",
  "evals": [
    {
      "id": 0,
      "prompt": "I want to ask Copilot why my JWT validation is failing intermittently, but I don't want to paste my entire src/ directory. Here are my files:\n\n// src/middleware/auth.ts\nimport { verifyToken } from '../utils/jwt';\nimport { UserService } from '../services/user.service';\nimport { Request, Response, NextFunction } from 'express';\n\nexport async function authMiddleware(req: Request, res: Response, next: NextFunction) {\n  const token = req.headers.authorization?.split(' ')[1];\n  if (!token) return res.status(401).json({ error: 'No token' });\n  try {\n    const payload = await verifyToken(token);\n    req.user = await UserService.findById(payload.sub);\n    next();\n  } catch (e) {\n    res.status(401).json({ error: 'Invalid token' });\n  }\n}\n\n// src/utils/jwt.ts\nimport jwt from 'jsonwebtoken';\nconst SECRET = process.env.JWT_SECRET || 'dev-secret';\n\nexport async function verifyToken(token: string) {\n  return jwt.verify(token, SECRET);\n}\n\nexport async function signToken(payload: object, expiresIn = '1h') {\n  return jwt.sign(payload, SECRET, { expiresIn });\n}\n\n// src/services/user.service.ts\nimport { db } from '../db';\n\nexport class UserService {\n  static async findById(id: string) {\n    return db.query('SELECT * FROM users WHERE id = $1', [id]);\n  }\n  static async findByEmail(email: string) {\n    return db.query('SELECT * FROM users WHERE email = $1', [email]);\n  }\n  static async updateLastLogin(id: string) {\n    return db.query('UPDATE users SET last_login = NOW() WHERE id = $1', [id]);\n  }\n  static async deactivate(id: string) {\n    return db.query('UPDATE users SET active = false WHERE id = $1', [id]);\n  }\n  static async listAll() {\n    return db.query('SELECT id, email, active FROM users ORDER BY created_at DESC');\n  }\n}\n\n// src/routes/user.routes.ts\nimport { Router } from 'express';\nimport { authMiddleware } from '../middleware/auth';\nimport { UserService } from '../services/user.service';\n\nconst router = Router();\nrouter.get('/me', authMiddleware, async (req, res) => {\n  res.json(req.user);\n});\nrouter.get('/users', authMiddleware, async (req, res) => {\n  res.json(await UserService.listAll());\n});\nexport default router;",
      "expected_output": "A trimmed context bundle including auth.ts (full), verifyToken from jwt.ts (the relevant function, not signToken), and findById from user.service.ts (not the other 4 methods). Routes file should be omitted as not relevant to JWT validation. Should show meaningful reduction percentage.",
      "files": []
    },
    {
      "id": 1,
      "prompt": "I'm trying to understand how my payment processing pipeline handles retries. Can you trim the context down so I can ask Copilot about it? Here's the relevant code:\n\n// src/payments/processor.ts\nimport { PaymentGateway } from './gateway';\nimport { RetryPolicy } from '../utils/retry';\nimport { logger } from '../utils/logger';\nimport { PaymentRecord } from '../models/payment';\n\nexport class PaymentProcessor {\n  private gateway: PaymentGateway;\n  private retryPolicy: RetryPolicy;\n\n  constructor() {\n    this.gateway = new PaymentGateway();\n    this.retryPolicy = new RetryPolicy({ maxAttempts: 3, backoffMs: 1000 });\n  }\n\n  async processPayment(amount: number, customerId: string, metadata: object) {\n    return this.retryPolicy.execute(async () => {\n      const result = await this.gateway.charge(amount, customerId);\n      await PaymentRecord.create({ amount, customerId, result, metadata });\n      return result;\n    });\n  }\n\n  async refund(paymentId: string, amount?: number) {\n    const record = await PaymentRecord.findById(paymentId);\n    return this.gateway.refund(record.gatewayId, amount ?? record.amount);\n  }\n\n  async getHistory(customerId: string, limit = 50) {\n    return PaymentRecord.find({ customerId, limit });\n  }\n}\n\n// src/utils/retry.ts\nexport class RetryPolicy {\n  private maxAttempts: number;\n  private backoffMs: number;\n\n  constructor({ maxAttempts, backoffMs }: { maxAttempts: number; backoffMs: number }) {\n    this.maxAttempts = maxAttempts;\n    this.backoffMs = backoffMs;\n  }\n\n  async execute<T>(fn: () => Promise<T>): Promise<T> {\n    let lastError: Error;\n    for (let attempt = 1; attempt <= this.maxAttempts; attempt++) {\n      try {\n        return await fn();\n      } catch (err) {\n        lastError = err as Error;\n        if (attempt < this.maxAttempts) {\n          await new Promise(r => setTimeout(r, this.backoffMs * attempt));\n        }\n      }\n    }\n    throw lastError!;\n  }\n}\n\n// src/payments/gateway.ts\nimport Stripe from 'stripe';\nconst stripe = new Stripe(process.env.STRIPE_KEY!, { apiVersion: '2023-10-16' });\n\nexport class PaymentGateway {\n  async charge(amount: number, customerId: string) {\n    return stripe.paymentIntents.create({ amount, currency: 'usd', customer: customerId, confirm: true });\n  }\n  async refund(gatewayId: string, amount: number) {\n    return stripe.refunds.create({ payment_intent: gatewayId, amount });\n  }\n  async getPaymentIntent(id: string) {\n    return stripe.paymentIntents.retrieve(id);\n  }\n}\n\n// src/models/payment.ts\nimport { db } from '../db';\nexport class PaymentRecord {\n  static async create(data: object) { return db.insert('payments', data); }\n  static async findById(id: string) { return db.findOne('payments', { id }); }\n  static async find(query: object) { return db.find('payments', query); }\n  static async updateStatus(id: string, status: string) { return db.update('payments', { id }, { status }); }\n}",
      "expected_output": "Bundle should include: processPayment() from processor.ts (the retry-relevant method), full RetryPolicy class from retry.ts (central to the question), gateway.charge() signature from gateway.ts (called by the retry path). Should omit: refund(), getHistory(), PaymentRecord model methods beyond create(), getPaymentIntent().",
      "files": []
    }
  ]
}
