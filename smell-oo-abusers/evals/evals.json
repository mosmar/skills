{
  "skill_name": "smell-oo-abusers",
  "evals": [
    {
      "id": 0,
      "prompt": "Can you do an OO abusers analysis on this TypeScript file?\n\n```typescript\n// discount.service.ts\nexport class DiscountService {\n  calculate(order: Order): number {\n    if (order.customerType === 'guest') {\n      return 0;\n    } else if (order.customerType === 'member') {\n      return order.subtotal * 0.05;\n    } else if (order.customerType === 'premium') {\n      return order.subtotal * 0.15;\n    } else if (order.customerType === 'vip') {\n      return order.subtotal * 0.25 + (order.subtotal > 500 ? 50 : 0);\n    } else {\n      return 0;\n    }\n  }\n\n  getLabel(customerType: string): string {\n    if (customerType === 'guest') return 'No discount';\n    else if (customerType === 'member') return '5% member discount';\n    else if (customerType === 'premium') return '15% premium discount';\n    else if (customerType === 'vip') return '25% VIP discount + bonus';\n    else return '';\n  }\n\n  isEligibleForFreeShipping(order: Order): boolean {\n    if (order.customerType === 'guest') return false;\n    else if (order.customerType === 'member') return order.subtotal > 100;\n    else if (order.customerType === 'premium') return order.subtotal > 50;\n    else if (order.customerType === 'vip') return true;\n    else return false;\n  }\n}\n\n// notification.service.ts\nexport class NotificationService {\n  send(userId: string, message: string, channel: string, options?: any): void {\n    if (channel === 'email') {\n      this.emailClient.send(userId, message, options?.subject);\n    } else if (channel === 'sms') {\n      const truncated = message.length > 160 ? message.slice(0, 157) + '...' : message;\n      this.smsClient.send(userId, truncated);\n    } else if (channel === 'push') {\n      this.pushClient.send(userId, options?.title ?? 'Notification', message);\n    } else if (channel === 'slack') {\n      this.slackClient.postMessage(userId, message, options?.threadId);\n    }\n  }\n}\n```",
      "expected_output": "Should find Switch Statements (Severe — customerType if/else chain duplicated 3 times in DiscountService, channel dispatch in NotificationService). Should recommend Replace Conditional with Polymorphism for DiscountService (abstract CustomerType class with calculate/getLabel/isEligibleForFreeShipping), and discriminated union + type-narrowing handlers or Strategy pattern for NotificationService. Should include TypeScript before/after sketches showing the polymorphism refactor.",
      "files": []
    },
    {
      "id": 1,
      "prompt": "OO abusers deep-dive on this TypeScript code:\n\n```typescript\n// document.processor.ts\nexport class DocumentProcessor {\n  private _parsedContent: ParsedContent | null = null;\n  private _validationErrors: string[] | null = null;\n  private _processingContext: ProcessingContext | null = null;\n  private _outputPath: string | null = null;\n\n  constructor(private readonly filePath: string) {}\n\n  async parse(): Promise<void> {\n    const raw = await fs.readFile(this.filePath, 'utf8');\n    this._parsedContent = await this.parseRaw(raw);\n  }\n\n  async validate(): Promise<boolean> {\n    if (!this._parsedContent) throw new Error('Must call parse() first');\n    this._validationErrors = await this.runValidations(this._parsedContent);\n    return this._validationErrors.length === 0;\n  }\n\n  async process(context: ProcessingContext): Promise<void> {\n    if (!this._parsedContent) throw new Error('Must call parse() first');\n    if (this._validationErrors === null) throw new Error('Must call validate() first');\n    this._processingContext = context;\n    this._outputPath = await this.runProcessing(this._parsedContent, context);\n  }\n\n  getOutput(): string {\n    if (!this._outputPath) throw new Error('Must call process() first');\n    return this._outputPath;\n  }\n}\n\n// shape.ts\nexport abstract class Shape {\n  abstract area(): number;\n  abstract perimeter(): number;\n  abstract color: string;\n  abstract borderWidth: number;\n  abstract render(ctx: CanvasRenderingContext2D): void;\n  abstract serialize(): object;\n  abstract clone(): Shape;\n  abstract toSvg(): string;\n  abstract toBitmap(): ImageData;\n}\n\nexport class Circle extends Shape {\n  color = 'black';\n  borderWidth = 1;\n  constructor(public radius: number) { super(); }\n  area() { return Math.PI * this.radius ** 2; }\n  perimeter() { return 2 * Math.PI * this.radius; }\n  render(ctx: CanvasRenderingContext2D) { /* ... */ }\n  serialize() { return { type: 'circle', radius: this.radius }; }\n  clone() { return new Circle(this.radius); }\n  toSvg() { return `<circle r=\"${this.radius}\"/>`; }\n  toBitmap(): ImageData { throw new Error('Not implemented'); }\n}\n```",
      "expected_output": "Should find: Temporary Field (Severe — DocumentProcessor has 4 nullable fields only valid after sequential method calls; recommend Extract Class to split into parsing/validation/processing stages with explicit state, or state machine type), Refused Bequest (Circle.toBitmap() throws 'Not implemented' — classic refused bequest; recommend Extract Interface splitting Shape into Renderable + Serializable + Geometric, or Replace Inheritance with Delegation). TypeScript-specific sketches showing discriminated union state machine for DocumentProcessor and interface segregation for Shape.",
      "files": []
    }
  ]
}
